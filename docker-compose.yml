version: '3.8'

services:
  portfolio-api:
    container_name: api-compose-damian
    image: djuarez88/devops-portfolio:latest
    ports:
      - "8080:5000"
    environment:
      - APP_ENV=production
      - APP_VERSION=1.0.5
    restart: always